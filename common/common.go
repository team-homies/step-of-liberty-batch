package common

import "main/model"

// 사건 저장 grpc (임시)
func SetEvents(atHistory []model.AtHistory) (mapData string, err error) {
	return
}

// 사건 위치 저장 grpc (임시)
func SetHistories(mapData string) (err error) {
	return
}
